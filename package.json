{
  "name": "pnda-se",
  "private": true,
  "version": "2.0.0",
  "description": "PNDA — Plateforme de Suivi & Évaluation (React/TS + Node/TS + Supabase)",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "concurrently -n api,web -c cyan,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix backend run build && npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
